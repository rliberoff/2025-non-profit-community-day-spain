namespace Demo.Agent.Fundraising.Models;

/// <summary>
/// Represents the role of a user in the fundraising system.
/// </summary>
public enum UserRole
{
    /// <summary>
    /// Campaign coordinator with full permissions (Coordinadora).
    /// </summary>
    Coordinator,

    /// <summary>
    /// Volunteer team member (Voluntario).
    /// </summary>
    Volunteer
}
